### Quick one:

``` bash
brew install fluidsynth
brew install node # or use nvm v16
npm install
#start fluidsynth in other terminal
#fluidsynth -i -s -o "shell.port=9977" -g 8 ./SalC5Light2.sf2
npm start
```
